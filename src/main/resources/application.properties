# Catalog Service Configuration
db.host=${POSTGRES_HOSTNAME:localhost}
db.port=${POSTGRES_PORT_CONTAINER:5432}
db.user=${POSTGRES_USER:myreadings-user}
db.password=${POSTGRES_PASSWORD:supersecretpassword}

quarkus.devservices.enabled=false
quarkus.application.name=catalog-service
quarkus.http.port=8082
app.repository.type=jpa
app.search.enrichment-strategy=${APP_SEARCH_ENRICHMENT_STRATEGY:normal}
smallrye.jwt.path.groups=realm_access/roles

%dev.quarkus.oidc.auth-server-url=http://${KEYCLOAK_HOSTNAME:localhost}:${KEYCLOAK_PORT_CONTAINER:8080}/realms/${KEYCLOAK_REALM:my-readings}
%dev.quarkus.oidc.token.issuer=http://localhost:${KEYCLOAK_PORT_HOST:8080}/realms/${KEYCLOAK_REALM:my-readings}
%prod.quarkus.oidc.auth-server-url=http://${KEYCLOAK_HOSTNAME:localhost}:${KEYCLOAK_PORT_CONTAINER:8080}/realms/${KEYCLOAK_REALM:my-readings}
%prod.quarkus.oidc.token.issuer=http://localhost:${KEYCLOAK_PORT_HOST:8080}/realms/${KEYCLOAK_REALM:my-readings}
quarkus.oidc.client-id=${KEYCLOAK_CLIENT_ID:myreadings-client}
quarkus.oidc.credentials.secret=${KC_ADMIN_PASSWORD:admin}
quarkus.oidc.tls.verification=none

quarkus.datasource."books-db".db-kind=postgresql
quarkus.datasource."books-db".username=${db.user}
quarkus.datasource."books-db".password=${db.password}
quarkus.datasource."books-db".jdbc.max-size=40
quarkus.datasource."books-db".jdbc.min-size=10
quarkus.datasource."books-db".jdbc.acquisition-timeout=5
quarkus.hibernate-orm."books-db".datasource=books-db
quarkus.hibernate-orm."books-db".packages=org.modular.playground.catalog.infrastructure.persistence.postgres
quarkus.hibernate-orm."books-db".jdbc.batch-size=25
quarkus.hibernate-orm."books-db".properties."hibernate.default_batch_fetch_size"=16

%dev.quarkus.datasource."books-db".jdbc.url=jdbc:postgresql://${db.host}:${db.port}/books_db
%prod.quarkus.datasource."books-db".jdbc.url=jdbc:postgresql://${db.host}:${db.port}/books_db

quarkus.otel.service.name=myreadings-catalog-service
quarkus.otel.exporter.otlp.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
quarkus.otel.exporter.otlp.traces.protocol=grpc
quarkus.otel.logs.enabled=true
quarkus.otel.exporter.otlp.logs.protocol=grpc
quarkus.otel.metrics.enabled=true
quarkus.datasource."books-db".jdbc.telemetry=true

quarkus.oidc.token-cache.max-size=100

quarkus.log.level=INFO
quarkus.log.category."org.hibernate".level=WARN
quarkus.log.console.json=true
quarkus.log.console.json.pretty-print=false

%dev.quarkus.devservices.enabled=false
%dev.quarkus.http.cors=true
%dev.quarkus.http.cors.origins=/.*/
%dev.quarkus.log.console.json=false
%dev.quarkus.log.console.json.pretty-print=false
